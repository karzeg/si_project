{% include 'default/_form.html.twig' with {
    page_title: 'author_edit',
    page_title_param: {'%id%': author.id},
    attr: {
        method: 'PUT',
        action: url('author_edit', {'id': author.id}),
    },
    form: form,
    back_to_list_path: 'author_index',
} %}