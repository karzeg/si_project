{% extends 'base.html.twig' %}

{% block title %}
    {{ 'book_create'|trans }}
{% endblock %}

{% block body %}
    <h1>{{ 'book_create'|trans }}</h1>
    {{ form_start(form, { method: 'POST', action: url('book_create') }) }}
    {{ form_widget(form) }}
    <div class="form-group row float-sm-right">
        <input type="submit" value="{{ 'action_save'|trans }}" class="btn btn-info" />
    </div>
    <div class="form-group row float-sm-left">
        <a href="{{ url('book_index') }}" class="btn btn-info">
            {{ 'action_back_to_list'|trans }}
        </a>
    </div>
    {{ form_end(form) }}
{% endblock %}