{% include 'default/_form.html.twig' with {
    page_title: 'book_edit',
    page_title_param: {'%id%': book.id},
    attr: {
        method: 'PUT',
        action: url('book_edit', {'id': book.id}),
    },
    form: form,
    back_to_list_path: 'book_index',
} %}